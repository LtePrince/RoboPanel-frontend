# Baseline defaults. Runtime overrides are saved to browser localStorage via the
# Settings page (analogous to BlogManager's manager-settings.json).

# Real robot backend (RoboPanel-backend Go API), full /api/v1 prefix.
VITE_REAL_API_URL=http://localhost:8088/api/v1

# Simulation video source base URL (optional; sim connection TBD).
VITE_SIM_VIDEO_BASE=
